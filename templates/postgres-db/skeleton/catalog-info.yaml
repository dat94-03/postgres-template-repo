apiVersion: scaffolder.backstage.io/v1beta3
kind: Component
metadata:
  name: ${{ values.name }}
  description: "Postgres database service ${{ values.name }}"
  annotations:
    mycompany.com/docker-container-name: ${{ values.name }}
spec:
  type: service
  owner: NGT-Service
  lifecycle: experimental
